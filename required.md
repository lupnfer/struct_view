原始需求：
1. 我想实现个结构体（安防场景视频提取的结构化数据）自动转成字符串的框架，基于上游提供的C的结构化数据，按用户诉求，转成字符串数据，
2. 用户需要描述字符串数据是按啥顺序组合起来的（因此需要结构体里的数据都会关联成一个name，name不仅仅支持单个节点，还要支持一些关键结构体），各个字段见的连接符符是啥（服务提供），还有些通用的设备信息
3. 实现要满足易拓展，架构良好同时用户易上手